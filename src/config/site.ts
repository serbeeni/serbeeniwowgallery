export const VERSION = 'v3.2.3'

/** Distance from the bottom of the page that triggers the next Tumblr page. */
export const INFINITE_SCROLL_OFFSET = 900

/** scrollY past which the back-to-top button fades in. */
export const BACK_TO_TOP_THRESHOLD = 400

/** Delay before scrolling to a post picked in grid view, so the stream can lay out first. */
export const SCROLL_TO_POST_DELAY = 50
